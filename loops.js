
let base = prompt("what number do you want to a multification table of?")
prompt
let range = promt("What number should I stop at?")

if(!base)
 base = 1;
}

for(let i = 1; i <= range; i++) {
      //loop body console.log(`$(base) x ${i} = ${base *i}`);
}  console.log (` ${base } .x ${i} =${base * i}`);
for (let i = i; i <= 10; i += 2) {
    console.log(i);
}